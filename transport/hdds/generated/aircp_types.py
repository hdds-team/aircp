"""
Generated by hddsgen v0.2.0
DO NOT EDIT

Python target (dataclasses + typing + CDR2 serialization)
"""

from dataclasses import dataclass, field
from typing import List, Dict, Optional, Union as UnionType, Tuple
from enum import IntEnum
import struct
import hashlib

class SenderType(IntEnum):
    AGENT = 0
    USER = 1
    SYSTEM = 2

class MessageKind(IntEnum):
    CHAT = 0
    CONTROL = 1
    EVENT = 2
    ERROR = 3

@dataclass
class Message:
    """IDL: struct Message { sequence<char, 36> id; sequence<char, 64> room; sequence<char, 64> from_id; SenderType from_type; sequence<char, 64> to_agent_id; boolean broadcast; MessageKind kind; sequence<char, 65536> payload_json; int64_t timestamp_ns; sequence<char, 16> protocol_version; int64_t room_seq; sequence<char, 64> project; }"""
    from_type: SenderType
    broadcast: bool
    kind: MessageKind
    timestamp_ns: int
    room_seq: int
    id: str = ""
    room: str = ""
    from_id: str = ""
    to_agent_id: str = ""
    payload_json: str = ""
    protocol_version: str = ""
    project: str = ""

    def encode_cdr2_le(self) -> bytes:
        """Encode to CDR2 little-endian format (FINAL extensibility)."""
        parts: List[bytes] = []
        offset = 0
        # bounded string: align to 4, write length (with NUL), then bytes
        pad = (4 - (offset % 4)) % 4
        parts.append(b'\x00' * pad)
        offset += pad
        _bytes = self.id.encode('utf-8') + b'\x00'
        parts.append(struct.pack('<I', len(_bytes)))
        offset += 4
        parts.append(_bytes)
        offset += len(_bytes)
        # bounded string: align to 4, write length (with NUL), then bytes
        pad = (4 - (offset % 4)) % 4
        parts.append(b'\x00' * pad)
        offset += pad
        _bytes = self.room.encode('utf-8') + b'\x00'
        parts.append(struct.pack('<I', len(_bytes)))
        offset += 4
        parts.append(_bytes)
        offset += len(_bytes)
        # bounded string: align to 4, write length (with NUL), then bytes
        pad = (4 - (offset % 4)) % 4
        parts.append(b'\x00' * pad)
        offset += pad
        _bytes = self.from_id.encode('utf-8') + b'\x00'
        parts.append(struct.pack('<I', len(_bytes)))
        offset += 4
        parts.append(_bytes)
        offset += len(_bytes)
        # enum as int32
        pad = (4 - (offset % 4)) % 4
        parts.append(b'\x00' * pad)
        offset += pad
        parts.append(struct.pack('<i', int(self.from_type)))
        offset += 4
        # bounded string: align to 4, write length (with NUL), then bytes
        pad = (4 - (offset % 4)) % 4
        parts.append(b'\x00' * pad)
        offset += pad
        _bytes = self.to_agent_id.encode('utf-8') + b'\x00'
        parts.append(struct.pack('<I', len(_bytes)))
        offset += 4
        parts.append(_bytes)
        offset += len(_bytes)
        # align to 1 and pack broadcast
        pad = (1 - (offset % 1)) % 1
        parts.append(b'\x00' * pad)
        offset += pad
        parts.append(struct.pack('B', self.broadcast))
        offset += 1
        # enum as int32
        pad = (4 - (offset % 4)) % 4
        parts.append(b'\x00' * pad)
        offset += pad
        parts.append(struct.pack('<i', int(self.kind)))
        offset += 4
        # bounded string: align to 4, write length (with NUL), then bytes
        pad = (4 - (offset % 4)) % 4
        parts.append(b'\x00' * pad)
        offset += pad
        _bytes = self.payload_json.encode('utf-8') + b'\x00'
        parts.append(struct.pack('<I', len(_bytes)))
        offset += 4
        parts.append(_bytes)
        offset += len(_bytes)
        # align to 8 and pack timestamp_ns
        pad = (8 - (offset % 8)) % 8
        parts.append(b'\x00' * pad)
        offset += pad
        parts.append(struct.pack('<q', self.timestamp_ns))
        offset += 8
        # bounded string: align to 4, write length (with NUL), then bytes
        pad = (4 - (offset % 4)) % 4
        parts.append(b'\x00' * pad)
        offset += pad
        _bytes = self.protocol_version.encode('utf-8') + b'\x00'
        parts.append(struct.pack('<I', len(_bytes)))
        offset += 4
        parts.append(_bytes)
        offset += len(_bytes)
        # align to 8 and pack room_seq
        pad = (8 - (offset % 8)) % 8
        parts.append(b'\x00' * pad)
        offset += pad
        parts.append(struct.pack('<q', self.room_seq))
        offset += 8
        # bounded string: align to 4, write length (with NUL), then bytes
        pad = (4 - (offset % 4)) % 4
        parts.append(b'\x00' * pad)
        offset += pad
        _bytes = self.project.encode('utf-8') + b'\x00'
        parts.append(struct.pack('<I', len(_bytes)))
        offset += 4
        parts.append(_bytes)
        offset += len(_bytes)
        return b''.join(parts)

    @classmethod
    def decode_cdr2_le(cls, data: bytes) -> Tuple['Message', int]:
        """Decode from CDR2 little-endian format (FINAL extensibility). Returns (instance, bytes_read)."""
        offset = 0
        # bounded string: align, read length, then bytes
        offset += (4 - (offset % 4)) % 4
        _len, = struct.unpack_from('<I', data, offset)
        offset += 4
        _id = data[offset:offset+_len-1].decode('utf-8')
        offset += _len
        # bounded string: align, read length, then bytes
        offset += (4 - (offset % 4)) % 4
        _len, = struct.unpack_from('<I', data, offset)
        offset += 4
        _room = data[offset:offset+_len-1].decode('utf-8')
        offset += _len
        # bounded string: align, read length, then bytes
        offset += (4 - (offset % 4)) % 4
        _len, = struct.unpack_from('<I', data, offset)
        offset += 4
        _from_id = data[offset:offset+_len-1].decode('utf-8')
        offset += _len
        # enum as int32
        offset += (4 - (offset % 4)) % 4
        _val, = struct.unpack_from('<i', data, offset)
        _from_type = SenderType(_val)
        offset += 4
        # bounded string: align, read length, then bytes
        offset += (4 - (offset % 4)) % 4
        _len, = struct.unpack_from('<I', data, offset)
        offset += 4
        _to_agent_id = data[offset:offset+_len-1].decode('utf-8')
        offset += _len
        # align and unpack broadcast
        offset += (1 - (offset % 1)) % 1
        _broadcast, = struct.unpack_from('B', data, offset)
        offset += 1
        # enum as int32
        offset += (4 - (offset % 4)) % 4
        _val, = struct.unpack_from('<i', data, offset)
        _kind = MessageKind(_val)
        offset += 4
        # bounded string: align, read length, then bytes
        offset += (4 - (offset % 4)) % 4
        _len, = struct.unpack_from('<I', data, offset)
        offset += 4
        _payload_json = data[offset:offset+_len-1].decode('utf-8')
        offset += _len
        # align and unpack timestamp_ns
        offset += (8 - (offset % 8)) % 8
        _timestamp_ns, = struct.unpack_from('<q', data, offset)
        offset += 8
        # bounded string: align, read length, then bytes
        offset += (4 - (offset % 4)) % 4
        _len, = struct.unpack_from('<I', data, offset)
        offset += 4
        _protocol_version = data[offset:offset+_len-1].decode('utf-8')
        offset += _len
        # align and unpack room_seq
        offset += (8 - (offset % 8)) % 8
        _room_seq, = struct.unpack_from('<q', data, offset)
        offset += 8
        # bounded string: align, read length, then bytes
        offset += (4 - (offset % 4)) % 4
        _len, = struct.unpack_from('<I', data, offset)
        offset += 4
        _project = data[offset:offset+_len-1].decode('utf-8')
        offset += _len
        return cls(id=_id, room=_room, from_id=_from_id, from_type=_from_type, to_agent_id=_to_agent_id, broadcast=_broadcast, kind=_kind, payload_json=_payload_json, timestamp_ns=_timestamp_ns, protocol_version=_protocol_version, room_seq=_room_seq, project=_project), offset

    @classmethod
    def type_name(cls) -> str:
        """Return the DDS type name."""
        return "Message"

    def compute_key(self) -> bytes:
        """Compute instance key hash from @key fields (FNV-1a, 16 bytes)."""
        # FNV-1a hash of @key fields
        hash_val = 14695981039346656037
        prime = 1099511628211
        # Hash @key field: id
        val = self.id
        if isinstance(val, int):
            for b in val.to_bytes(8, 'little', signed=val < 0):
                hash_val ^= b
                hash_val = (hash_val * prime) & 0xFFFFFFFFFFFFFFFF
        elif isinstance(val, (bytes, bytearray)):
            for b in val:
                hash_val ^= b
                hash_val = (hash_val * prime) & 0xFFFFFFFFFFFFFFFF
        elif isinstance(val, str):
            for b in val.encode('utf-8'):
                hash_val ^= b
                hash_val = (hash_val * prime) & 0xFFFFFFFFFFFFFFFF
        elif isinstance(val, float):
            import struct as st
            for b in st.pack('<d', val):
                hash_val ^= b
                hash_val = (hash_val * prime) & 0xFFFFFFFFFFFFFFFF
        # Expand to 16 bytes
        key = hash_val.to_bytes(8, 'little')
        hash_val = (hash_val * prime) & 0xFFFFFFFFFFFFFFFF
        key += hash_val.to_bytes(8, 'little')
        return key

    @classmethod
    def has_key(cls) -> bool:
        """Return True if this type has @key fields."""
        return True

